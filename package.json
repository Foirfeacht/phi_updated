{
  "name": "phi-dynamic-directives-seed",
  "private": true,
  "version": "0.0.1",
  "description": "A starter project for PHI dynamic directives",
  "devDependencies": {
    "bower": "^1.3.1",
    "grunt": "~0.4.5",
    "grunt-contrib-compress": "^0.13.0",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "grunt restoreDefaultPhiPayloadData && node ./scripts/web-server.js"
  }
}
